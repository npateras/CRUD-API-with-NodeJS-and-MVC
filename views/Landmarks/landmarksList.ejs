<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-8">
                <h4 class="font-weight-bold">All Landmarks</h4>
            </div>
            <div class="col-4">
                <a href="./addLandmark" class="btn btn-success float-right" style="float: right;"><i class="fa-solid fa-plus"></i> Add New</a>
            </div>
        </div>
    </div>
    <div class="card-body">
        <table class="table table-hover">
            <thead class="table-dark">
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Country</th>
                    <th scope="col">Date Added</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if(landmarks.length > 0) { %>
                    <% for(let landmark of landmarks) { %>
                    <tr>
                        <td><%= landmark.name %></td>
                        <td><%= landmark.country %></td>
                        <td><%= landmark.date_added %></td>
                        <td>
                            <a href="./updateLandmark/<%= landmark._id %>" class="btn btn-sm btn-warning"><i class="fa-solid fa-pen-to-square"></i></a>
                            <a href="./deleteLandmark/<%= landmark._id %>" class="btn btn-sm btn-danger"><i class="fa-solid fa-trash-can"></i></a>
                        </td>
                    </tr>
                    <% } %>
                <% } else { %>
                    <tr>
                        <td>No landmarks found</td>
                        <td></td>
                        <td></td>
                    </tr>
                <% } %>
            </tbody>
            </table>
    </div>
</div>